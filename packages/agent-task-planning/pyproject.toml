[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-task-planning"
version = "1.0.0"
description = "AI reliability framework with confidence extraction and outlier mining."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Members of the PDA Task Force" }
]
keywords = [
    "ai",
    "agents",
    "llm",
    "planning",
    "task-management",
    "claude",
    "gpt-4",
    "gemini"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "pydantic>=2.0.0",
    "httpx>=0.25.0",
    "structlog>=23.0.0",
]

[project.optional-dependencies]
anthropic = ["anthropic>=0.18.0"]
openai = ["openai>=1.0.0"]
google = ["google-generativeai>=0.3.0"]
ollama = ["ollama>=0.1.0"]
confidence = []
confidence-full = [
    "numpy>=1.20.0",
    "sentence-transformers>=2.0.0",
]
mining = [
    "numpy>=1.20.0",
    "sentence-transformers>=2.0.0",
]
mining-full = [
    "numpy>=1.20.0",
    "sentence-transformers>=2.0.0",
    "umap-learn>=0.5.0",
    "hdbscan>=0.8.0",
    "scikit-learn>=1.0.0",
]
all = [
    "anthropic>=0.18.0",
    "openai>=1.0.0",
    "google-generativeai>=0.3.0",
    "ollama>=0.1.0",
    "numpy>=1.20.0",
    "sentence-transformers>=2.0.0",
    "umap-learn>=0.5.0",
    "hdbscan>=0.8.0",
    "scikit-learn>=1.0.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "pre-commit>=3.0.0",
]

[project.urls]
Homepage = "https://github.com/PDA-Task-Force/pda-platform"
Documentation = "https://github.com/PDA-Task-Force/pda-platform/tree/main/packages/agent-task-planning"
Repository = "https://github.com/PDA-Task-Force/pda-platform"
Issues = "https://github.com/PDA-Task-Force/pda-platform/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_planning"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.10"
strict = true
