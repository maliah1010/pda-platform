[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pm-data-tools"
version = "1.1.0"
description = "Parser, validator, and SQLite assurance store for UK government project data. Supports NISTA, GMPP, MSPDI, Primavera P6, and Jira. Backbone of the PDA Platform MCP server."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Ant Newman", email = "antjsnewman@outlook.com"},
]
maintainers = [
    {name = "Ant Newman", email = "antjsnewman@outlook.com"},
]
keywords = [
    "project-management",
    "data-interoperability",
    "mspdi",
    "primavera",
    "p6",
    "jira",
    "nista",
    "gmpp",
    "assurance",
    "ipa",
    "gate-readiness",
    "benefits-realisation",
    "risk-register",
    "earned-value",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Other Audience",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Scheduling",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "lxml>=5.0.0",
    "click>=8.0.0",
    "pydantic>=2.0.0",
    "python-dateutil>=2.8.0",
    "httpx[http2]>=0.25.0",
    "agent-task-planning>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=0.23.0",
    "mypy>=1.8.0",
    "ruff>=0.1.0",
    "pre-commit>=3.0.0",
    "httpx>=0.25.0",
    "types-python-dateutil>=2.8.0",
]
assurance = [
    "structlog>=23.0.0",
]
all = [
    "httpx>=0.25.0",
    "structlog>=23.0.0",
]

[project.scripts]
pm-data-tools = "pm_data_tools.cli:main"

[project.urls]
Homepage = "https://github.com/antnewman/pda-platform"
Documentation = "https://github.com/antnewman/pda-platform/tree/main/docs"
Repository = "https://github.com/antnewman/pda-platform"
Issues = "https://github.com/antnewman/pda-platform/issues"
Changelog = "https://github.com/antnewman/pda-platform/releases"

[tool.hatch.build.targets.wheel]
packages = ["src/pm_data_tools"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = [
    "--cov=pm_data_tools",
    "--cov-report=term-missing",
    "--cov-report=html",
    "--cov-fail-under=100",
    "--strict-markers",
    "-vv",
]

[tool.coverage.run]
branch = true
source = ["src/pm_data_tools"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
    "@abstractmethod",
]
fail_under = 100

[tool.ruff]
line-length = 120
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
]
ignore = ["E501", "E722", "E741", "E402", "B904", "SIM108", "SIM102", "SIM116", "N802", "B017"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.isort]
known-first-party = ["pm_data_tools"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_any_unimported = false
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
check_untyped_defs = true
strict_equality = true

[[tool.mypy.overrides]]
module = "lxml.*"
ignore_missing_imports = true
